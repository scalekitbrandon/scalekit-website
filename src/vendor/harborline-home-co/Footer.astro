---
// Harborline Home Co. — Footer. Same shape as Anchor's Footer.astro,
// reskinned to teal/sand + Fraunces logo wordmark.
interface LinkGroup {
  heading: string;
  links: { label: string; href: string }[];
}

interface Props {
  businessName?: string;
  tagline?: string;
  phone?: string;
  phoneHref?: string;
  groups?: LinkGroup[];
}

const {
  businessName = 'Harborline Home Co.',
  tagline = 'Home services, clearly priced — licensed, insured, and locally owned.',
  phone = '(555) 234-6789',
  phoneHref = 'tel:5552346789',
  groups = [
    {
      heading: 'Company',
      links: [
        { label: 'Home', href: '#home' },
        { label: 'About Us', href: '#about' },
        { label: 'Service Areas', href: '#service-areas' },
      ],
    },
    {
      heading: 'Services',
      links: [
        { label: 'Exterior Painting', href: '#services' },
        { label: 'Roof Repair', href: '#services' },
        { label: 'Decks & Patios', href: '#services' },
      ],
    },
    {
      heading: 'Get Started',
      links: [{ label: 'Get My Estimate', href: '#estimate' }],
    },
  ],
} = Astro.props;

const year = new Date().getFullYear();
---

<footer class="hl-footer">
  <div class="footer-inner">
    <div class="footer-top">
      <div class="footer-brand">
        <p class="footer-logo">{businessName}</p>
        <p class="footer-tagline">{tagline}</p>
        <a href={phoneHref} class="footer-phone">{phone}</a>
      </div>

      <div class="footer-groups">
        {groups.map((group) => (
          <div class="footer-group">
            <h4>{group.heading}</h4>
            <ul>
              {group.links.map((link) => (
                <li><a href={link.href}>{link.label}</a></li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </div>

    <div class="footer-bottom">
      <p class="footer-copyright">&copy; {year} {businessName}. All rights reserved.</p>
    </div>
  </div>
</footer>

<style>
  .hl-footer {
    font-family: 'Inter', -apple-system, sans-serif;
    background: #083D3D;
    color: #ffffff;
    padding: 64px 40px 32px;
  }
  .footer-inner { max-width: 1400px; margin: 0 auto; }

  .footer-top {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 48px;
    margin-bottom: 48px;
  }

  .footer-brand { max-width: 300px; flex-shrink: 0; }
  .footer-logo {
    font-family: 'Fraunces', serif;
    font-size: 20px;
    font-weight: 600;
    color: #ffffff;
    margin: 0 0 12px;
  }
  .footer-tagline {
    font-size: 14px;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.6);
    margin: 0 0 14px;
  }
  .footer-phone {
    display: inline-block;
    font-size: 15px;
    font-weight: 700;
    color: #F2C79A;
    text-decoration: none;
  }

  .footer-groups { display: flex; flex-wrap: wrap; gap: 48px; }
  .footer-group { min-width: 130px; }
  .footer-group h4 {
    font-family: 'Fraunces', serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: #ffffff;
    margin: 0 0 16px;
  }
  .footer-group ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .footer-group a {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.65);
    text-decoration: none;
  }
  .footer-group a:hover { color: #F2C79A; }

  .footer-bottom {
    padding-top: 24px;
    border-top: 1px solid rgba(255, 255, 255, 0.12);
  }
  .footer-copyright {
    font-size: 13px;
    color: rgba(255, 255, 255, 0.45);
    margin: 0;
  }

  @media (max-width: 560px) {
    .hl-footer { padding: 48px 20px 24px; }
    .footer-brand { max-width: none; width: 100%; }
  }
</style>
